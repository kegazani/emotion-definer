# Emotion Diary - Личный дневник с анализом эмоций

Веб-приложение для ведения личного дневника с автоматическим анализом эмоционального состояния на основе ML-моделей и красивым glassmorphism интерфейсом в стиле Apple.

## Возможности

- 📝 **Дневник в формате чата** - удобный интерфейс для записи мыслей
- 🤖 **ML анализ эмоций** - автоматическое определение эмоционального состояния по тексту
- 📊 **Статистика и отчёты** - графики за день, неделю и месяц
- ⌚ **Данные здоровья** - интеграция данных с часов
- 🎨 **Glass UI** - современный дизайн в стиле Apple с эффектом стекла

## Технологии

### Backend
- Python 3.9+
- FastAPI
- SQLAlchemy
- Transformers (BERT для анализа эмоций)
- SQLite

### Frontend
- React 18
- TypeScript
- Vite
- Recharts
- date-fns

## Установка и запуск

### Backend

1. Перейдите в директорию backend:
```bash
cd backend
```

2. Создайте виртуальное окружение:
```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Запустите сервер:
```bash
uvicorn main:app --reload --port 8000
```

Backend будет доступен по адресу: http://localhost:8000

### Frontend

1. Перейдите в директорию frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите dev сервер:
```bash
npm run dev
```

Frontend будет доступен по адресу: http://localhost:5173

## Использование

1. Откройте приложение в браузере
2. Перейдите на страницу "Дневник"
3. Начните записывать свои мысли - эмоции будут определяться автоматически
4. Просматривайте статистику на странице "Статистика"
5. Просматривайте данные с часов на странице "⌚ Часы"

## API Endpoints

### Дневник
- `POST /api/entries` - Создать запись дневника
- `GET /api/entries` - Получить записи

### Статистика
- `GET /api/stats/daily` - Статистика за день
- `GET /api/stats/weekly` - Статистика за неделю
- `GET /api/stats/monthly` - Статистика за месяц

### Данные здоровья
- `POST /api/health-data` - Добавить данные здоровья
- `GET /api/health-data` - Получить данные здоровья

### Данные с часов (Watch API)
- `POST /api/watch` - Отправить данные с часов (один замер)
- `POST /api/watch/batch` - Отправить пакет данных с часов
- `GET /api/watch` - Получить данные с часов
- `GET /api/watch/latest` - Получить последние данные
- `GET /api/watch/analytics` - Получить аналитику (period=day|week|month)

#### Формат данных с часов (POST /api/watch)
```json
{
  "device_id": "watch-001",
  "heart_rate": 72,
  "hrv": 45.5,
  "spo2": 98,
  "stress_level": 35,
  "steps": 1500,
  "calories": 150,
  "distance": 1.2,
  "active_minutes": 25,
  "sleep_hours": 7.5,
  "sleep_quality": 85,
  "body_battery": 70,
  "skin_temperature": 36.6,
  "respiratory_rate": 16
}
```

#### Деплой бекенда
Для запуска на внешнем IP:
```bash
uvicorn main:app --host 0.0.0.0 --port 8000
```

## iOS приложение для Apple Watch

Для передачи данных с Apple Watch на бекенд нужно iOS приложение. Пример кода находится в папке `ios-app/`.

### Быстрый старт

1. **Создайте проект в Xcode:**
   - Откройте Xcode
   - File → New → Project → iOS App
   - Назовите "EmotionDiaryWatch"

2. **Настройте HealthKit:**
   - В Signing & Capabilities добавьте HealthKit
   - Добавьте в Info.plist описания доступа (см. `ios-app/Info.plist.example`)

3. **Скопируйте файлы:**
   - `HealthKitManager.swift` - работа с HealthKit
   - `ContentView.swift` - интерфейс
   - `EmotionDiaryWatchApp.swift` - точка входа

4. **Установите URL бекенда:**
   - В `HealthKitManager.swift` замените `YOUR_IP` на IP вашего сервера

5. **Запустите приложение:**
   - Разрешите доступ к HealthKit
   - Нажмите "Синхронизировать сейчас"

### Тестирование без iOS

Для тестирования API можно использовать скрипт:

```bash
cd ios-app
python3 test-script.py
```

Не забудьте заменить `YOUR_IP` в скрипте на IP вашего бекенда.

Подробная инструкция: [ios-app/README.md](ios-app/README.md)

## Структура проекта

```
emotion-diary/
├── backend/
│   ├── main.py              # FastAPI приложение
│   ├── models.py            # SQLAlchemy модели
│   ├── emotion_analyzer.py  # ML анализ эмоций
│   ├── schemas.py           # Pydantic схемы
│   └── requirements.txt
├── frontend/
│   ├── src/
│   │   ├── components/      # React компоненты
│   │   ├── pages/           # Страницы приложения
│   │   ├── styles/          # CSS стили
│   │   └── api/             # API клиент
│   └── package.json
├── ios-app/                 # iOS приложение для Apple Watch
│   ├── HealthKitManager.swift
│   ├── ContentView.swift
│   ├── EmotionDiaryWatchApp.swift
│   ├── test-script.py
│   └── README.md
└── README.md
```

## Примечания

- При первом запуске ML-модель будет загружена автоматически (может занять некоторое время при первом использовании)
- База данных SQLite создаётся автоматически при первом запуске
- Если ML-модель не загрузится, приложение будет использовать простой анализ по ключевым словам
- Данные с часов отправляются через API эндпоинт `/api/watch`

## Решение проблем

### Ошибка "Не удалось сохранить запись"

1. **Проверьте, что backend запущен:**
   ```bash
   cd backend
   uvicorn main:app --reload --port 8000
   ```

2. **Проверьте подключение:**
   ```bash
   python check_connection.py
   ```

3. **Проверьте логи backend** - там будет детальная информация об ошибке

4. **Проверьте, что порт 8000 свободен:**
   ```bash
   lsof -i :8000  # macOS/Linux
   ```

5. **Если ML-модель не загружается:**
   - Убедитесь, что установлены все зависимости: `pip install -r requirements.txt`
   - При первом запуске модель скачивается автоматически (может занять время)
   - Приложение будет работать с упрощённым анализом по ключевым словам

### Проблемы с CORS

Если frontend не может подключиться к backend, проверьте настройки CORS в `backend/main.py`

